version: '3.8'

networks:
  seekmem-network:
    driver: bridge
    name: seekmem-network

services:
  seekmem-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: oceanbase/seekmem-server:latest
    container_name: seekmem-server
    ports:
      - "${SEEKMEM_SERVER_PORT:-8000}:8000"
    environment:
      - SEEKMEM_SERVER_HOST=${SEEKMEM_SERVER_HOST:-0.0.0.0}
      - SEEKMEM_SERVER_PORT=${SEEKMEM_SERVER_PORT:-8000}
      - SEEKMEM_SERVER_WORKERS=${SEEKMEM_SERVER_WORKERS:-4}
      - SEEKMEM_SERVER_API_KEYS=${SEEKMEM_SERVER_API_KEYS:-}
      - SEEKMEM_SERVER_AUTH_ENABLED=${SEEKMEM_SERVER_AUTH_ENABLED:-false}
      - SEEKMEM_SERVER_RATE_LIMIT_ENABLED=${SEEKMEM_SERVER_RATE_LIMIT_ENABLED:-true}
      - SEEKMEM_SERVER_RATE_LIMIT_PER_MINUTE=${SEEKMEM_SERVER_RATE_LIMIT_PER_MINUTE:-100}
      - SEEKMEM_SERVER_LOG_LEVEL=${SEEKMEM_SERVER_LOG_LEVEL:-INFO}
      - SEEKMEM_SERVER_LOG_FORMAT=${SEEKMEM_SERVER_LOG_FORMAT:-json}
      - SEEKMEM_SERVER_CORS_ENABLED=${SEEKMEM_SERVER_CORS_ENABLED:-true}
      - SEEKMEM_SERVER_CORS_ORIGINS=${SEEKMEM_SERVER_CORS_ORIGINS:-*}
      - SEEKMEM_DATABASE_URL=${SEEKMEM_DATABASE_URL:-}
      # Database configuration for connecting to seekdb
      # Use SEEKDB_ROOT_PASSWORD variable for unified password management
      - DATABASE_PROVIDER=oceanbase
      - OCEANBASE_HOST=seekdb
      - OCEANBASE_PORT=2881
      - OCEANBASE_USER=root@sys
      - OCEANBASE_PASSWORD=${SEEKDB_ROOT_PASSWORD:-}
      - OCEANBASE_DATABASE=seekmem
      - OCEANBASE_COLLECTION=memories
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/system/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      - seekdb
    networks:
      - seekmem-network

  seekdb:
    image: oceanbase/seekdb:latest
    container_name: seekmem-seekdb
    ports:
      - "2881:2881"
      - "2886:2886"
    environment:
      # Unified password variable for both seekdb and SeekMem Server
      - ROOT_PASSWORD=${SEEKDB_ROOT_PASSWORD:-}
      - CPU_COUNT=4
      - MEMORY_LIMIT=4G
      - LOG_DISK_SIZE=2G
      - DATAFILE_SIZE=2G
      - DATAFILE_NEXT=2G
      - DATAFILE_MAXSIZE=50G
      - SEEKDB_DATABASE=seekmem
    volumes:
      - seekdb_data:/var/lib/oceanbase
      # Mount init.sql for database initialization (optional)
      # - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "if [ -z \"${SEEKDB_ROOT_PASSWORD}\" ]; then mysqladmin ping -h localhost -P 2881 -u root; else mysqladmin ping -h localhost -P 2881 -u root -p${SEEKDB_ROOT_PASSWORD}; fi"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - seekmem-network

volumes:
  seekdb_data:

